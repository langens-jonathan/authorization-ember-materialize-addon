{{#if isScreen0Active}}

{{md-btn text='GROUPS'
	action='moveToScreen'
	class=buttonClass
	actionArg="0"
	}}
{{md-btn text='USERS'
	action='moveToScreen'
	class=buttonClass
	actionArg="1"
	}}
{{groups-list openGroupDetail=(action "openGroupDetail")
	      openNewGroup=(action "openNewGroup")
	      buttonClass=buttonClass
	      tableClass=tableClass}}

{{/if}}

{{#if isScreen1Active}}
{{md-btn text='GROUPS'
	action='moveToScreen'
	class=buttonClass
	actionArg="0"
	}}
{{md-btn text='USERS'
	action='moveToScreen'
	class=buttonClass
	actionArg="1"
	}}
{{users-list openUserDetail=(action "openUserDetail")
	      openNewUser=(action "openNewUser")
	      buttonClass=buttonClass
	      tableClass=tableClass}}	
{{/if}}

{{#if isScreen2Active}}
{{md-btn text='BACK'
	action='moveToScreen'
	class=buttonClass
	actionArg="0"
	}}
{{group-display group=currentModel manageAuthorizationsGroup=(action "manageAuthorizationsGroup")
	      buttonClass=buttonClass
	      tableClass=tableClass}}
{{/if}}

{{#if isScreen3Active}}
{{md-btn text='BACK'
	action='moveToScreen'
	class=buttonClass
	actionArg="1"
	}}
{{user-display user=currentModel manageAuthorizationsUser=(action "manageAuthorizationsUser")
	      buttonClass=buttonClass
	      tableClass=tableClass}}
{{/if}}

{{#if isScreen4Active}}
{{md-btn text='BACK'
	action='moveToScreen'
	class=buttonClass
	actionArg="2"
	}}
{{authorizations-list authority=currentModel
	      buttonClass=buttonClass
	      tableClass=tableClass}}
{{/if}}

{{#if isScreen5Active}}
{{md-btn text='BACK'
	action='moveToScreen'
	class=buttonClass
	actionArg="3"
	}}
{{authorizations-list authority=currentModel
	      buttonClass=buttonClass
	      tableClass=tableClass}}
{{/if}}

{{#if isScreen6Active}}
{{md-btn text='BACK'
	action='moveToScreen'
	class=buttonClass
	actionArg="0"
	}}
{{new-group moveToScreen=(action "moveToScreen") buttonClass=buttonClass}}	
{{/if}}

{{#if isScreen7Active}}
{{md-btn text='BACK'
	action='moveToScreen'
	class=buttonClass
	actionArg="1"
	}}
{{new-user moveToScreen=(action "moveToScreen") buttonClass=buttonClass}}
{{/if}}